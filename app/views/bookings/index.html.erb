<p style="color: green"><%= notice %></p>

<h1>Bookings</h1>


<div class="container">
    <div class="row">
      <% @bookings.each do |booking| %>
        <%= render booking %>
      <% end %>
    </div>
</div>

<%= link_to "New booking", new_booking_path %>

